export default class Api {
  async makeRequest(apiRequest) {
    // if token is not there or value, get a new token. 
    const isValidToken = true

    if (isValidToken) {
      return apiRequest
    }
  }
}